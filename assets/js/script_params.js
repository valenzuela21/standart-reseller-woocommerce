let id_shop = object_param.id_shopping;
let taxonomy = object_param.taxonomy;
let term = object_param.term;
let id_reseller = object_param.id_reseller;

export const app = { id_shop, taxonomy, term, id_reseller}

